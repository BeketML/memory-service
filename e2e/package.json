{
  "name": "memory-service-e2e",
  "version": "1.0.0",
  "description": "Playwright API tests for the memory service (task.md contract)",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0"
  }
}
